import React from "react";

const Original = (props) => {
  return (
    <>
      {/* {props.origin.map((orig) => (
        <li>{orig}</li>
      ))} */}
      {props.origin}
    </>
  );
};

export default Original;
